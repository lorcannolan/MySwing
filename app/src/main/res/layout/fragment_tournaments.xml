<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <android.support.design.widget.TabLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/tournament_tab_navigation"
        app:tabBackground="@color/places_ui_default_primary"
        app:tabTextColor="@color/homeBackground"
        app:tabIndicatorColor="@color/homeBackground">
    </android.support.design.widget.TabLayout>

    <android.support.v4.view.ViewPager
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/tournament_tab_container"
        android:layout_below="@+id/tournament_tab_navigation">
    </android.support.v4.view.ViewPager>

    <ru.dimorinny.floatingtextbutton.FloatingTextButton
        android:id="@+id/new_tournament"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:floating_background_color="@color/homeBackground"
        app:floating_left_icon="@drawable/ic_add_icon"
        app:floating_title="New Tournament"
        app:floating_title_color="@android:color/white"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"/>
</RelativeLayout>